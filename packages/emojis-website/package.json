{
  "name": "@quotidianlabs/emojis-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "parcel index.html --no-autoinstall",
    "build": "SITE_COMMIT_SHA=$(git rev-parse --short HEAD 2>/dev/null || true) parcel build index.html --no-autoinstall --public-url ./ --dist-dir dist"
  },
  "devDependencies": {
    "posthtml-extend": "0.6.5",
    "posthtml-include": "2.0.1"
  }
}
